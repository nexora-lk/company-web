# lib/

Pure utility functions. No React, no JSX.

Examples: `cn.ts` (className merger), `format.ts` (date/currency formatters), `slug.ts`.

If a file imports from `react`, it belongs in `hooks/` or `components/`, not here.
