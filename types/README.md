# types/

Shared TypeScript types and interfaces used across multiple folders.

Domain-specific types should stay next to their data (e.g. `Director` lives in `data/team.ts`).
Put truly cross-cutting types here — e.g. `ApiResponse`, `Locale`, `Nullable`.
